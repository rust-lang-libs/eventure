[package]
name = "examples"
version = "0.0.5"
edition = "2021"

[[example]]
name = "in-memory"
path = "src/in_memory/main.rs"

[dependencies]
eventure = { path = "../sdk" }
uuid = { version = "1.8.0", features = ["v4"] }
